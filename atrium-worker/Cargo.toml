[package]
name = "atrium-worker"
version.workspace = true
edition.workspace = true
description = "Worker asynchrone pour l'IA d'accueil Atrium — planifie et declenche la meteo de l'ambiance du serveur"

[dependencies]
platform-common-worker = { path = "../platform-common-worker" }
serde = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

[lints]
workspace = true
