# Point d'entree unique de la stack DiscordSentinel.
# Requiert Docker Compose >= 2.20 pour le support de `include`.
name: discordsentinel

include:
  - compose.core.yml
  - compose.auth.yml
  - compose.atrium.yml
  - compose.nexus.yml
  - compose.observability.yml
  - compose.tls.yml
