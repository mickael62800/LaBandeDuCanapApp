[package]
name = "nexus-gateway"
version = "0.1.0"
edition = "2021"
description = "Gateway de la plateforme jeux Nexus — point d'entree reseau (scaffold)"

[dependencies]
tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

[lints]
workspace = true
