#[tokio::test]
async fn enable_slowmode() {
    assert!(true);
}

#[tokio::test]
async fn disable_slowmode() {
    assert!(true);
}

#[tokio::test]
async fn get_slowmode_config() {
    assert!(true);
}
