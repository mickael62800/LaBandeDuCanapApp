// Enums du domain regroupes par bounded context.
pub mod community;
pub mod moderation;
pub mod system;

// Re-exports plats (chaque enum etait avant accessible via
// `crate::sentinel::domain::enums::Type` (re-export plat aussi disponible).
